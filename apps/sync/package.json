{
  "name": "@fissa/sync",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "types": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "pnpx ts-node src/index.ts",
    "start": "pm2 start ecosystem.config.js",
    "postinstall": "npm install -g pm2 && sudo pm2 install typescript && sudo pm2 install @types/node && sudo pm2 install ts-node"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fissa/api": "*",
    "@fissa/utils": "*",
    "@trpc/client": "^10.12.0",
    "node-cron": "^3.0.2",
    "spotify-web-api-node": "^5.0.2"
  },
  "devDependencies": {
    "@types/node-cron": "^3.0.7",
    "eslint-config-acme": "0.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  }
}
