{
  "name": "@fissa/sync",
  "version": "1.0.0",
  "description": "Syncing currently playing tracks in spotify with the fissa playlists",
  "main": "index.ts",
  "types": "index.ts",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --outfile=dist/index.js",
    "clean": "rm -rf node_modules",
    "test": "vitest",
    "start": "node --experimental-specifier-resolution=node --max_old_space_size=100 dist",
    "dev": "pnpm with-env node --experimental-specifier-resolution=node dist",
    "debug": "pnpm with-env node --experimental-specifier-resolution=node --trace_gc --max_old_space_size=300 --inspect dist",
    "with-env": "dotenv -e ../../.env --"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fissa/api": "*",
    "@fissa/utils": "*",
    "@trpc/client": "^10.21.1",
    "croner": "^6.0.3",
    "esbuild": "^0.17.18"
  },
  "devDependencies": {
    "@fissa/test": "*",
    "@types/node-cron": "^3.0.7",
    "dotenv": "^16.0.3",
    "eslint-config-acme": "0.0.0",
    "nodemon": "^2.0.22",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  }
}
